import React from 'react';

const Blog = () => {
    return (
        <div className='grid lg:grid-cols-2 md:grid-cols-1 gap-3 mt-10 border'>
            <div className='shadow-lg shadow-slate-400 p-4 mt-4'>
                <h2 className='font-bold text-2xl capitalize '>what is purpose to use router in react</h2>
                <p>React Router is a standard library for routing in React. It enables the navigation among views of various components in a React Application, allows changing the browser URL, and keeps the UI in sync with the URL.</p>
            </div>
            <div className='shadow-lg shadow-slate-400 p-4 mt-4 border'>
                <h2 className='font-bold text-2xl capitalize '>what is purpose to use router in react</h2>
                <p>React Router is a standard library for routing in React. It enables the navigation among views of various components in a React Application, allows changing the browser URL, and keeps the UI in sync with the URL.</p>
            </div>
            <div className='shadow-lg shadow-slate-400 p-4 mt-4 border'>
                <h2 className='font-bold text-2xl capitalize '>what is purpose to use router in react</h2>
                <p>React Router is a standard library for routing in React. It enables the navigation among views of various components in a React Application, allows changing the browser URL, and keeps the UI in sync with the URL.</p>
            </div>
        </div>
    );
};

export default Blog;